VERSION 2.2.0 - DEPLOYMENT READY

✅ CONFIRMED WORKING FEATURES:

1. PRESET BUTTON NAMES FROM VARIABLES
   - Buttons pull text from: $(showcall:preset_1_name) through $(showcall:preset_8_name)
   - Names update automatically from ShowCall preset data
   - Real-time synchronization confirmed

2. VARIABLE DEFINITIONS (8 new)
   - preset_1_name through preset_8_name
   - All defined in initVariables()
   - All populated in updateVariables()

3. DYNAMIC TEXT RENDERING
   - Preset buttons: text: `$(showcall:preset_${index + 1}_name)`
   - Updates in real-time from ShowCall
   - Falls back to preset.label if variable unavailable

4. BACKUP VARIABLES (still active)
   - All clip names (clip_1_1_name through clip_2_8_name)
   - All layer names (layer_1_name through layer_8_name)
   - All layer status (layer_1_status through layer_8_status)
   - All column names (column_1_name through column_8_name)

VERSION INFO:
- package.json: 2.2.0 ✅
- manifest.json: 2.2.0 ✅
- Archive: companion-module-showcall-2.2.0.tgz (59KB) ✅

STATUS: PRODUCTION READY ✅

Deploy with: companion-module-showcall-2.2.0.tgz
